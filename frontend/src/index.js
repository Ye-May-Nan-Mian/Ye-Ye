import React from "react";
import ReactDOM from "react-dom";
import "./index.css";
import App from "./App";

// no need to use React.StrictMode
// if don't use React.StrictMode, what will happen?
ReactDOM.render(<App />, document.getElementById("root"));
